# 本機人機協作專案管理系統

> 給單一決策者與本機 AI Agent 使用的任務系統。人作為決策者，Agent 作為執行者。

---

## 已拍板決策

| 項目 | 決策 |
|------|------|
| 產品形態 | 本機 Web 服務（瀏覽器 UI + localhost API） |
| 儲存 | 混合：workspace 內 markdown 為任務 source of truth，SQLite 做索引與租約 |
| Agent 權限 | 可自建任務；其餘仍為執行者（認領、進度、完成、退回；不能發布草稿／取消／驗收） |
| 驗收 | 不新增第六狀態；`done` + `human_reviewed` 旗標 |
| 套件管理 | npm |
| 運行模式 | `manual`（現有）+ `auto`（協調者編排）；編碼仍外部 Cursor/Skill |
| 協調者 | 進程內 LangGraph 風格編排 + GLM（OpenAI 兼容）；任務帳本仍為 SoT |
| AI 員工 | 名冊一等公民；`system_prompt` 必填；`assignable` 控制可否被協調者分派 |
| 分派/審查 | 任務 `assignee_*` / `queue_order` / `review.*`；五欄看板疊加展示 |

---

## 1. 產品定位

- **人**：開專案、寫規格、發布任務、驗收、打回、取消
- **Agent**：經 API 拉待處理佇列、認領、回報、宣稱完成；不內建 LLM
- **不登入**；假設只有一個本機使用者
- **專案** = 綁定一個本機資料夾當 workspace

第一階段完成定義：Windows 上能走通「建專案並綁資料夾 → 人寫草稿並發布 → Skill/API 讀取並改為完成 → 人驗收或打回」。

---

## 2. 第一階段範圍

**做：** 多專案、五狀態任務、人驗收旗標、活動日誌、本機 REST、Cursor Skill、看板+列表、workspace 遺失時重新定位。

**不做：** 內建 Agent、登入/雲端、Electron/Tauri、評論串、甘特圖、子任務實體、附件表、Webhook/WebSocket、任意檔案 CRUD、自動 git commit/merge、多人指派、優先級 UI、拖曳改狀態。

**做（代碼隔離）：** 有 git 的專案在任務發布（或 Agent 直接建為 `todo`）且 **使用者勾選 `use_isolation`** 時，由 PM-AI 建立 git worktree + 任務 branch；任務帳本仍在主 workspace；仍不自動 commit/merge。

---

## 3. 技術棧

- 執行期：Node.js >= 22.13（含 22 LTS 與 24，內建 sqlite 無需 experimental flag）+ TypeScript + npm
- HTTP：Hono（REST + OpenAPI）
- 校驗：Zod
- DB：SQLite + Drizzle + Node 內建 `node:sqlite`（不用 better-sqlite3，避免 Windows 編譯原生模組）
- UI：Vite + React + Tailwind CSS + shadcn/ui
- 契約：OpenAPI 3，掛在 `/api/v1/openapi.json`

啟動：`npm run dev` / `npm start`，只聽 `127.0.0.1:7432`。

目錄：

```
src/server/   — Hono、狀態機、檔案+SQLite 雙寫
src/web/      — React 畫面
src/shared/   — Zod schema、狀態枚舉
skill/        — Cursor Skill
```

---

## 4. 系統架構

```
決策者瀏覽器 ──同源 REST──► Hono (127.0.0.1:7432)
Cursor Skill ──Bearer REST──►
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
              狀態機與權限    workspace/.pm-ai    AppData SQLite
                    │               │               │
                    └───────────────┴───────────────┘
                                    │
                              路徑守衛 → 綁定的專案資料夾
```

- 任務帳本：`<workspace>/.pm-ai/tasks/*.md`
- 索引/租約/專案清單：`%APPDATA%/pm-ai/pm-ai.sqlite`（不進 git）
- 本機設定與 token：`%APPDATA%/pm-ai/config.json`
- Agent **禁止**直接改 `.pm-ai/tasks`；改狀態只走 API
- 啟動或開啟專案時：掃描任務檔 → 對帳 SQLite（檔案贏）

安全：只綁 `127.0.0.1`、Host 白名單、CORS 不 `*`、除 `/health` 外都要 Bearer token。

---

## 5. 資料落地

### Workspace（進 git）

```
<workspace>/.pm-ai/
  project.yml
  README.md
  tasks/TASK-0001.md
  activity/2026-08.jsonl
  comments/TASK-0001.jsonl
  locks/          # gitignore
  cache/          # gitignore
```

### 任務檔格式

YAML frontmatter + markdown 正文。關鍵欄位：

- `id`, `uid`, `title`, `status`, `version`, `human_reviewed`
- `claimed_by`, `acceptance_criteria`, `goal`, `constraints`
- `rejections[]`（打回原因列表）
- `git_branch`, `worktree_path`, `isolation_base_sha`, `isolation_status`, `isolation_error`, `use_isolation`（代碼任務隔離；使用者可選是否啟用）

### SQLite（AppData）

- `projects`：id、name、workspace_path、binding_status、archived
- `tasks`：索引入口 + rel_path + content_hash
- `leases`：task_id、agent_name、expires_at、lease_token
- `activity_logs`：可選索引
- `comments`：評論索引（檔案 `.pm-ai/comments/{taskId}.jsonl` 為 SoT）

寫入順序：短檔案鎖 → 檢查 version → 写 markdown → append jsonl → 更新 SQLite。衝突回 409。

### 狀態機

```
draft → todo → in_progress → done
draft → cancelled
todo → cancelled
in_progress → todo
in_progress → cancelled
done → todo          # 僅人：打回或重開
cancelled → todo     # 僅人
```

**人**：建草稿、發布、取消、重開、驗收通過、打回
**Agent**：建立任務（有驗收標準則直接為待處理，否則為草稿）、todo→in_progress（claim）、寫進度、in_progress→done、in_progress→todo（release）

`done` 且 `human_reviewed=false` 顯示「待你驗收」。

---

## 6. Agent API

基底：`http://127.0.0.1:7432/api/v1`，`Authorization: Bearer <token>`

### 共用

- `GET /health`
- `GET /meta`
- `GET /projects`, `GET /projects/:id`
- `GET /projects/:id/tasks`
- `GET /tasks/:id`

### 僅人

- `POST /projects`, `PATCH /projects/:id`, `POST /projects/:id/relocate`
- `PATCH /tasks/:id`
- `POST /tasks/:id/publish` | `cancel` | `reopen`
- `POST /tasks/:id/review/approve` | `review/reject`

### 人與 Agent

- `POST /projects/:id/tasks`（人永遠建為 draft；Agent 有驗收標準則建為 todo）
- `GET /tasks/:id/comments`、`POST /tasks/:id/comments`（自由評論，不需認領）

### 僅 Agent

- `GET /inbox`（只回 todo）
- `POST /tasks/:id/claim`
- `POST /tasks/:id/heartbeat`
- `POST /tasks/:id/progress`
- `POST /tasks/:id/complete`
- `POST /tasks/:id/release`

Claim 租約 15 分鐘，heartbeat 續租。

---

## 7. UI

- 導航：頂部專案切換 + 專案內左欄（總覽 / 任務 / 專案設定）
- 頁面：專案列表、專案首頁、任務看板/列表、任務詳情、設定
- 看板五欄；取消預設摺疊；不拖曳改狀態
- 草稿對 Agent 不可見；交出前至少標題 + 驗收標準必填
- 處理中規格鎖定；有處理中任務時 3 秒輪詢

---

## 8. 預設細節

- 埠固定 7432；token 放 AppData
- `.pm-ai/` 納入 git（locks/、cache/ ignore）
- 詳情用獨立頁
- 不要優先級、不要批次交出草稿
- 驗收通過不必逐條勾 checklist
- 取消的任務可以人重開回待處理
- 同時允許多筆「處理中」；首頁只突出最新一筆；**並行改檔**靠每任務 git worktree 隔離（外部 Agent 須在 `execution_path` 對應的 Cursor 視窗工作）
- 任務發布時建立 worktree（失敗則 `isolation_status=failed`，可重試）；**Git 收尾**（任務詳情「開發隔離」面板）：任務完成（`done`，含驗收通過後）且分支仍存在時可 **Merge 到目標分支**；驗收通過（`human_reviewed`）後可 **刪除 worktree** → **刪除 branch**（刪 branch 前檢查是否已合入目標分支；未 merge 拒絕）。**分支仍存在時可恢復 worktree**（僅恢復已 commit 內容）。Merge 衝突時 abort 並列出衝突檔，需到主 workspace 手動解決。API：`GET/POST /tasks/:id/git/*`
- **人可啟動/停止任務預覽服務**：啟動命令寫在專案 `.pm-ai/project.yml`（`preview_command`，預設 `npm run dev`）；PM-AI 在任務 `execution_path`（worktree 就緒則用 worktree）spawn 子進程，每任務獨立端口（`7500 + 任務序號`，占用則向後掃）；預覽 URL 為 `http://127.0.0.1:{port}`。worktree 無 `node_modules` 時可選先跑 `preview_install_command`（預設 `npm install`）。僅人可操作；Agent 不啟動預覽。清理 worktree 前若服務在跑會先停止。
- **待驗收時可查看 git diff（Split 並排）**：有 Git 隔離時對比 `isolation_base_sha` ↔ 任務 branch（含 worktree 未提交變更）；無隔離時對比主 workspace 工作區 ↔ `HEAD`（可能含與本任務無關改動，UI 會提示）。無 git 時 fallback 顯示 Agent 回報的 `artifacts`。API：`GET /tasks/:id/changes`、`GET /tasks/:id/changes/diff?path=`（僅人、僅待驗收）。
- **待驗收不保证已 commit**：Agent 完成时 PM-AI 不检查 git；Skill 建议 commit，但可能有 worktree 未提交改动。验收请结合 Diff 与调试预览。
- **项目总览可切换主 workspace branch**（需 git）：`GET/POST /projects/:id/git`；已在其他 worktree 检出的 branch 不可选。待验收页不提供 switch branch。

---

## 9. Auto 模式（協調者編排）

### 雙模式

| 模式 | 誰當 PM | 外部 AI |
|------|---------|---------|
| `manual` | 人 | 認領、實現、回報 |
| `auto` | 協調者 Orchestrator | 按分派認領；可有 reviewer 角色 |

專案欄位：`run_mode: manual | auto`。切換前若有進行中 Auto Run 需先 pause/stop。

### AI 員工名冊

- 欄位：`role`、`name`、`system_prompt`（必填）、`skills_tags`、`status`、`assignable`、`created_by`、`prompt_source`、`creation_rationale`
- 人可在「AI 員工」頁 CRUD；人手建預設 `assignable=false`，打開後協調者才可分派
- 協調者可按專案內容生成員工+提示詞（預設 `assignable=true`）
- 每個專案一個 `orchestrator` 角色（不分入普通分派池）

### 任務擴展（看板真相）

```yaml
assignee_agent_id / assignee_name  # 預先指派（≠ claimed_by）
queue_order                        # 同員工建議順序
review:
  required, reviewer_type (human|agent|orchestrator|none),
  reviewer_agent_id, status (none|pending|approved|rejected), note
```

`done` 後依 `review.*` 路由；`human` 對齊現有驗收；merge 主分支仍僅人。

### Auto Run / Decision / Meeting / Review Policy

- Run：`running|paused|awaiting_human|completed|stopped`；人可 pause/stop
- Decision Gate：選項 → 等人 → 喚醒編排
- Meeting：多角色結構化發言與紀要
- Review Policy：與人協定 AI 复查 vs 人把關；變更需人確認

### 協調者權限

可：建員工、建/發布任務、分派（僅 `assignable` 池）、開會、開 Gate、AI review、匯總。  
不可：繞過 `human_verify`、stop 後繼續跑、派給 `assignable=false`、直接改 `.pm-ai/tasks`。

### API 增量（摘要）

- `GET/POST /projects/:id/agents`，`GET/PATCH /agents/:id`，`POST /agents/:id/retire`
- `GET/POST /projects/:id/runs`，`POST /runs/:id/{pause,resume,stop,message}`
- `GET /projects/:id/decisions`，`POST /decisions/:id/resolve`
- `GET/POST /projects/:id/meetings`，`GET/PUT /projects/:id/review-policy`
- Inbox 支援 `?assignee_agent_id=` / `agent_name` 過濾；任務含 `system_prompt` 於員工詳情

### UI 增量

- 左欄：AI 員工、Auto 工作台
- 看板卡片：指派、queue_order、審查徽章
- Gate / Policy 確認頁

### 明確不做（Auto 一期）

無頭自動改倉、Deep Agents 替換帳本、協調者自動 merge 主分支、多人登入。
