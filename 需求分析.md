# 本機人機協作專案管理系統

> 給單一決策者與本機 AI Agent 使用的任務系統。人作為決策者，Agent 作為執行者。

---

## 已拍板決策

| 項目 | 決策 |
|------|------|
| 產品形態 | 本機 Web 服務（瀏覽器 UI + localhost API） |
| 儲存 | 混合：workspace 內 markdown 為任務 source of truth，SQLite 做索引與租約 |
| Agent 權限 | 嚴格執行者：只能認領待處理、寫進度、完成或退回 |
| 驗收 | 不新增第六狀態；`done` + `human_reviewed` 旗標 |
| 套件管理 | npm |

---

## 1. 產品定位

- **人**：開專案、寫規格、發布任務、驗收、打回、取消
- **Agent**：經 API 拉待處理佇列、認領、回報、宣稱完成；不內建 LLM
- **不登入**；假設只有一個本機使用者
- **專案** = 綁定一個本機資料夾當 workspace

第一階段完成定義：Windows 上能走通「建專案並綁資料夾 → 人寫草稿並發布 → Skill/API 讀取並改為完成 → 人驗收或打回」。

---

## 2. 第一階段範圍

**做：** 多專案、五狀態任務、人驗收旗標、活動日誌、本機 REST、Cursor Skill、看板+列表、workspace 遺失時重新定位。

**不做：** 內建 Agent、登入/雲端、Electron/Tauri、評論串、甘特圖、子任務實體、附件表、Webhook/WebSocket、任意檔案 CRUD、自動 git commit、多人指派、優先級 UI、拖曳改狀態。

---

## 3. 技術棧

- 執行期：Node 22 + TypeScript + npm
- HTTP：Hono（REST + OpenAPI）
- 校驗：Zod
- DB：SQLite + Drizzle + better-sqlite3
- UI：Vite + React + Tailwind CSS + shadcn/ui
- 契約：OpenAPI 3，掛在 `/api/v1/openapi.json`

啟動：`npm run dev` / `npm start`，只聽 `127.0.0.1:7432`。

目錄：

```
src/server/   — Hono、狀態機、檔案+SQLite 雙寫
src/web/      — React 畫面
src/shared/   — Zod schema、狀態枚舉
skill/        — Cursor Skill
```

---

## 4. 系統架構

```
決策者瀏覽器 ──同源 REST──► Hono (127.0.0.1:7432)
Cursor Skill ──Bearer REST──►
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
              狀態機與權限    workspace/.pm-ai    AppData SQLite
                    │               │               │
                    └───────────────┴───────────────┘
                                    │
                              路徑守衛 → 綁定的專案資料夾
```

- 任務帳本：`<workspace>/.pm-ai/tasks/*.md`
- 索引/租約/專案清單：`%APPDATA%/pm-ai/pm-ai.sqlite`（不進 git）
- 本機設定與 token：`%APPDATA%/pm-ai/config.json`
- Agent **禁止**直接改 `.pm-ai/tasks`；改狀態只走 API
- 啟動或開啟專案時：掃描任務檔 → 對帳 SQLite（檔案贏）

安全：只綁 `127.0.0.1`、Host 白名單、CORS 不 `*`、除 `/health` 外都要 Bearer token。

---

## 5. 資料落地

### Workspace（進 git）

```
<workspace>/.pm-ai/
  project.yml
  README.md
  tasks/TASK-0001.md
  activity/2026-08.jsonl
  locks/          # gitignore
  cache/          # gitignore
```

### 任務檔格式

YAML frontmatter + markdown 正文。關鍵欄位：

- `id`, `uid`, `title`, `status`, `version`, `human_reviewed`
- `claimed_by`, `acceptance_criteria`, `goal`, `constraints`
- `rejections[]`（打回原因列表）

### SQLite（AppData）

- `projects`：id、name、workspace_path、binding_status、archived
- `tasks`：索引入口 + rel_path + content_hash
- `leases`：task_id、agent_name、expires_at、lease_token
- `activity_logs`：可選索引

寫入順序：短檔案鎖 → 檢查 version → 写 markdown → append jsonl → 更新 SQLite。衝突回 409。

### 狀態機

```
draft → todo → in_progress → done
draft → cancelled
todo → cancelled
in_progress → todo
in_progress → cancelled
done → todo          # 僅人：打回或重開
cancelled → todo     # 僅人
```

**人**：建草稿、發布、取消、重開、驗收通過、打回
**Agent**：todo→in_progress（claim）、寫進度、in_progress→done、in_progress→todo（release）

`done` 且 `human_reviewed=false` 顯示「待你驗收」。

---

## 6. Agent API

基底：`http://127.0.0.1:7432/api/v1`，`Authorization: Bearer <token>`

### 共用

- `GET /health`
- `GET /meta`
- `GET /projects`, `GET /projects/:id`
- `GET /projects/:id/tasks`
- `GET /tasks/:id`

### 僅人

- `POST /projects`, `PATCH /projects/:id`, `POST /projects/:id/relocate`
- `POST /projects/:id/tasks`（永遠建為 draft）
- `PATCH /tasks/:id`
- `POST /tasks/:id/publish` | `cancel` | `reopen`
- `POST /tasks/:id/review/approve` | `review/reject`

### 僅 Agent

- `GET /inbox`（只回 todo）
- `POST /tasks/:id/claim`
- `POST /tasks/:id/heartbeat`
- `POST /tasks/:id/progress`
- `POST /tasks/:id/complete`
- `POST /tasks/:id/release`

Claim 租約 15 分鐘，heartbeat 續租。

---

## 7. UI

- 導航：頂部專案切換 + 專案內左欄（總覽 / 任務 / 專案設定）
- 頁面：專案列表、專案首頁、任務看板/列表、任務詳情、設定
- 看板五欄；取消預設摺疊；不拖曳改狀態
- 草稿對 Agent 不可見；交出前至少標題 + 驗收標準必填
- 處理中規格鎖定；有處理中任務時 3 秒輪詢

---

## 8. 預設細節

- 埠固定 7432；token 放 AppData
- `.pm-ai/` 納入 git（locks/、cache/ ignore）
- 詳情用獨立頁
- 不要優先級、不要批次交出草稿
- 驗收通過不必逐條勾 checklist
- 取消的任務可以人重開回待處理
- 同時允許多筆「處理中」；首頁只突出最新一筆
